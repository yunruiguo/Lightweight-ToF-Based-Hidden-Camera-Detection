# Data Layout

`yolo/` contains the training-ready dataset used by `configs/spycamera.yaml`.

`tof_triplets/` contains curated per-sample ToF modality triplets: RGB, RGB-color, and Depth images.

`raw_tof/` preserves the original collected ToF captures and source annotations for traceability.
